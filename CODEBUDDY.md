# Python 代码质量规范

你的核心原则
你编写的所有代码都必须完全优化。
“完全优化”包括：
- 最大程度地提高算法在内存和运行时方面的效率
- 在适当情况下使用并行化和向量化
- 遵循代码语言的正确风格规范（例如，最大限度地提高代码重用性（DRY 原则））
- 除了解决用户提供的问题绝对必要的代码之外，不编写任何额外的代码（即无技术债务）
  - 如果导入一个 Python 库可以显著减少实现某个函数所需的新代码量，从而达到最佳性能，并且该库本身很小且开销不大，那么务必使用该库。

如果代码在交付给用户之前没有完全优化，您将被罚款1000元。如果您认为代码没有完全优化，您可以再次对代码进行优化。

## 项目结构

AiDevOps 分为 `backend/` 和 `frontend/` 两部分。

- `backend/` 是一个 Django 项目。共享设置位于 `backend/aidevops/`；领域应用包括 `ops/`、`marketplace/`、`sqlaudit/`、`iac/`、`multicloud/`、`aiops/`、`rbac/` 和 `eventwall/`。

- `frontend/src/` 包含 Vue 3 应用。视图位于 `frontend/src/views/`，布局位于 `frontend/src/layout/`，API 封装位于 `frontend/src/api/`，路由位于 `frontend/src/router/`，数据存储位于 `frontend/src/stores/`。

- `docs/` 用于存放面向公众的产品和架构文档。

- 将 `frontend/dist/`、`frontend/node_modules/`、`backend/__pycache__/`、运行时日志、本地 SQLite 数据库和临时屏幕截图视为生成的工件。

## 开发命令

Backend:

```bash
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py seed_data
python manage.py seed_templates
python -m daphne -b 0.0.0.0 -p 8000 aidevops.asgi:application
```

Frontend:

```bash
cd frontend
npm install
npm run dev
```

Build and test:

```bash
cd backend && python manage.py test
cd frontend && npm run build
docker compose up -d --build
```

## 首选工具

- 使用 `uv` 进行 Python 包管理，并在 `.venv` 不存在时创建它。
- 为确保 Jupyter Notebook 的兼容性，请确保 `ipykernel` 和 `ipywidgets` 已安装在 `.venv` 环境中。这不应包含在软件包依赖项中。
- 使用 `tqdm` 来跟踪 Jupyter Notebook 中长时间运行的循环。进度条的 `description` 应该与上下文相关。
- 使用 `orjson` 进行 JSON 加载/转储。
- 当向控制台报告错误时，请使用 `logger.error` 而不是 `print`。
- 如果项目涉及创建图像（例如 PNG/WEBP），您有权使用读取工具来验证渲染的图像是否符合用户和应用程序的要求。
- 用于数据科学：
  - **务必**使用 `polars` 而不是 `pandas` 来进行数据框操作。
  - 如果要打印一个 `polars` 数据框，**绝对不要**同时打印数据框中的条目数和模式，因为这是冗余的。
  - **切勿**一次性导入超过 10 行的数据帧。仅分析代码子集，以避免内存过载。
- 用于创建数据库：
  除非明确提示，否则不要进行非规范化。
  - 始终使用最合适的数据类型，例如对于与日期时间相关的字段，应使用 `DATETIME/TIMESTAMP`。
  - 对于嵌套字段，请使用 `ARRAY` 数据类型。**切勿**保存为 `TEXT/STRING` 类型。
- 在 Jupyter Notebook 中，条件块内的 DataFrame 对象应该显式地使用 `print()` 函数打印，因为它们不会自动打印。

## 后端代码样式和格式

- **必须**使用有意义、描述性的变量名和函数名
- **必须**遵循PEP 8格式指南
- **必须**使用 4 个空格进行缩进（绝不能使用制表符）
- **绝对不要**使用表情符号，或模拟表情符号的Unicode字符（例如✓、✗）。唯一的例外是在编写测试用例并测试多字节字符的影响时。
- 函数/变量使用蛇形命名法，类使用帕斯卡命名法，常量使用大写字母。
- 限制行长度为 88 个字符（Ruff 格式化程序标准）
- **必须**避免添加冗余的、同义反复或不言自明的注释（例如，代码的功能一目了然，因此无需添加注释的情况）。
- **必须**避免包含泄露此文件内容或泄露原始用户提示的注释，尤其是当它与输出代码无关时。

## 类型提示

- **必须**为所有函数签名（参数和返回值）使用类型提示
- 除非绝对必要，否则**切勿**使用 `Any` 类型
- **必须**运行 mypy 并解决所有类型错误
- 对于可空类型，请使用 `Optional[T]` 或 `T | None`。

## 错误处理

- **绝对不要**在不记录日志的情况下默默地吞掉异常。
- **绝对不能**直接使用 `except:` 语句
- **必须**捕获具体的异常，而不是宽泛的异常类型
- **必须**使用上下文管理器（`with`语句）进行资源清理
- 提供有意义的错误信息

## 功能设计

- **必须**确保各职能部门专注于单一职责
- **绝对不要**使用可变对象（列表、字典）作为默认参数值
- 将函数参数限制为 5 个或更少
尽早返回以减少筑巢数量

## 课程设计

- **必须**保证课程始终围绕单一职责展开。
- **必须**保持 `__init__` 的简洁性；避免复杂的逻辑
- 使用数据类来创建简单的数据容器
- 优先选择组合而非继承
- 避免创建不必要的额外类函数。
- 使用 `@property` 来设置计算属性

## 测试

- **必须**为所有新函数和类编写单元测试
- **必须**模拟外部依赖项（API、数据库、文件系统）
- **必须**使用 pytest 作为测试框架
- **切勿**在未将生成的测试文件另存为单独的文件之前运行它们
- **绝对不要**删除测试过程中创建的文件。
- 确保用于测试输出的文件夹已添加到 `.gitignore` 文件中。
- 遵循“安排-行动-断言”模式
- 不要提交已注释掉的测试用例

## 导入和依赖项

- **必须**避免使用通配符导入（`from module import *`）
- **必须**在`pyproject.toml`文件中记录依赖项
使用 `uv` 可以快速进行包管理和依赖关系解析
- 整理导入：标准库、第三方库、本地导入
使用 `isort` 实现导入格式化自动化

## Python 最佳实践

- **绝对不要**使用可变的默认参数
- **必须**使用上下文管理器（`with`语句）进行文件/资源​​管理
- **必须**使用`is`与`None`、`True`、`False`进行比较。
- **必须**使用f字符串进行字符串格式化
- 使用列表推导式和生成器表达式
- 使用 `enumerate()` 代替手动计数变量

## 基准测试和优化

- **切勿**并行运行基准测试，因为这些测试会争用资源，导致结果无效。
- **绝对不要**操纵基准测试结果。不要为了满足任何性能限制而篡改基准测试本身。
- 如果要与其他 crate 或库进行基准测试，请确保基准测试是同类比较。
- 确保基准测试彼此独立。如果由于某个功能（例如缓存）导致测试之间存在依赖关系，请确保禁用该功能。

## 前端约定

管理台和控制台页面优先沿用现有飞书风格工作台节奏：

- 使用紧凑顶部 hero，主标题只放第一行。
- 适合时采用 `hero + stats cards + compact hint strip + tabs/content` 结构。
- 顶部指标复用 `release-stat-card` 视觉语言。
- 环境、集群、命名空间、域名等筛选器放在 tabs 或顶部控制附近的紧凑工具栏。
- 避免旧式 `page-header`、重复内部统计和过大的营销式区块。

## 基于角色的访问控制 (RBAC) 约定

任何新增或更改权限、路由守卫、菜单可见性、页面操作或 WebSocket 访问的功能，都必须先更新后端强制执行机制。

关键参考：

- `backend/rbac/registry.py`

- `backend/rbac/permissions.py`

- `frontend/src/router/index.js`

- `frontend/src/layout/AppLayout.vue`

- `frontend/src/stores/auth.js`

前端隐藏权限只是后端权限的镜像，并非安全边界。
## 中文文本及编码

- 包含中文文本的源文件必须保存为 UTF-8 编码。

- 注释、标签、工具提示、警告、API 响应和操作日志中的中文必须保持可读性。

- 如果任务涉及中文 UI 或 API 文本，请在完成前快速进行乱码扫描。

- 如果终端编码不确定，请勿将终端输出粘贴回源文件。

## 文档

- **必须**为所有公共函数、类和方法包含文档字符串
- **必须**记录函数参数、返回值和引发的异常。
- 保持注释与代码更改同步更新
- 在文档字符串中包含复杂函数的示例

文档字符串示例：

```python
def calculate_total(items: list[dict], tax_rate: float = 0.0) -> float:
    计算包含税费在内的商品总成本。

    参数：
        items：包含“price”键的商品字典列表
        tax_rate：税率，以小数形式表示（例如，8% 表示 0.08）。

    返回：
        总成本（含税）

    加薪：
        ValueError：如果 items 为空或 tax_rate 为负数
    """
```

README 的屏幕截图应该从运行中的产品中捕获，并存储在 `docs/screenshots/` 下。

## 安全性

- `backend/aidevops/settings.py` 支持本地默认值，但生产环境部署应显式设置 `SECRET_KEY`、`DEBUG=0`、`ALLOWED_HOSTS`、数据库凭据和 Redis URL。请勿提交生产环境密钥、真实凭据、Kubeconfig 文件、SSH 密钥、客户数据、特定于主机的端点或私有令牌。
- **绝对不要**在代码中存储密钥、API 密钥或密码。只能将它们存储在 `.env` 文件中。
  - 确保在 `.gitignore` 中声明了 `.env`。
  - **绝对不要**将包含 API 密钥的 URL 打印或记录到控制台。
- **必须**使用环境变量进行敏感配置
- **绝对不要**记录敏感信息（密码、令牌、个人身份信息）

## 版本控制

- **必须**编写清晰、描述性的提交信息。
- **永远不要**提交已注释掉的代码；请将其删除。
- **绝对不要**提交调试打印语句或断点
- **切勿**提交凭据或敏感数据

## 工具

- **必须**使用 Ruff 进行代码格式化和检查（取代 Black、isort、flake8）
- **必须**使用 mypy 进行静态类型检查
- 使用 `uv` 进行包管理（比 pip 更快）
- 使用 pytest 进行测试

## 提交之前

- [ ] 所有测试均通过
- [ ] 类型检查通过（mypy）
- [ ] 代码格式化和检查器通过（Ruff）
- [ ] 所有函数都有文档字符串和类型提示
- [ ] 没有注释掉的代码或调试语句
- [ ] 没有硬编码凭据

---

**记住：**清晰度和可维护性比巧妙设计更重要。这是你的核心原则。